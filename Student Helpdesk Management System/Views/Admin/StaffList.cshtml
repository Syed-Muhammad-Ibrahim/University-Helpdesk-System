@model IEnumerable<HelpdeskModel.Models.Staff>

@{
    ViewData["Title"] = "All Staff";
}

<div class="card">
    <h3>All Staff</h3>

    @{
        var currentSearch = ViewBag.CurrentSearch as string;
        long? currentDept = ViewBag.CurrentDept as long?;
        var currentStatus = ViewBag.CurrentStatus as string;

        var departments = ViewBag.Departments as IEnumerable<HelpdeskModel.Models.Department>
        ?? Enumerable.Empty<HelpdeskModel.Models.Department>();
    }

    <form method="get" class="row g-2 mb-3">
        <div class="col-md-4">
            <input type="text"
                   name="search"
                   value="@currentSearch"
                   class="form-control"
                   placeholder="Search by name, email...">
        </div>

        <div class="col-md-3">
            <select name="department" class="form-select">
                <option value="">All departments</option>
                @foreach (var dept in departments)
                {
                    <option value="@dept.Id" selected="@(currentDept.HasValue && dept.Id == currentDept.Value)">
                        @dept.Name
                    </option>
                }
            </select>
        </div>

        <div class="col-md-3">
            <select name="status" class="form-select">
                <option value="">All status</option>
                <option value="active" selected="@(currentStatus == "active")">Active</option>
                <option value="inactive" selected="@(currentStatus == "inactive")">InActive</option>
                <option value="deleted" selected="@(currentStatus == "deleted")">Deleted</option>
            </select>
        </div>

        <div class="col-md-1 d-grid">
            <button type="submit" class="btn btn-primary">Filter</button>
        </div>

        <div class="col-md-1 d-grid">
            <a asp-controller="Admin"
               asp-action="StaffList"
               class="btn btn-outline-secondary">
                Clear
            </a>
        </div>
    </form>

    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Department</th>
                <th>Phone</th>
                <th>Address</th>
                <th>Status</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var s in Model)
            {
                <tr>
                    <td>@s.Name</td>
                    <td>@s.User?.Email</td>
                    <td>@s.Department?.Name</td>
                    <td>@s.Phone</td>
                    <td>@s.Address</td>
                    <td>
                        @if (s.Status == HelpdeskModel.BusinessRules.ModelStatus.Active)
                        {
                            @:Active
                        }
                        else if (s.Status == HelpdeskModel.BusinessRules.ModelStatus.Deleted)
                        {
                            @:Deleted
                        }
                        else if (s.Status == HelpdeskModel.BusinessRules.ModelStatus.InActive)
                        {
                            @:InActive
                        }
                        else
                        {
                            @s.Status.ToString()
                        }
                    </td>
                    <td>
                        <a asp-controller="Admin"
                           asp-action="EditStaff"
                           asp-route-id="@s.Id"
                           class="btn btn-sm btn-primary">
                            Edit
                        </a>

                        <form asp-controller="Admin"
                              asp-action="DeleteStaff"
                              method="post"
                              style="display:inline;">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="id" value="@s.Id" />

                            <button type="submit"
                                    class="btn btn-sm btn-danger"
                                    onclick="return confirm('Are you sure you want to soft delete this staff?');">
                                Delete
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
